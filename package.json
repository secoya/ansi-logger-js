{
	"author": "Secoya A/S <npm@secoya.dk>",
	"bin": {
		"ansi-logger": "./bin/cli.js"
	},
	"bugs": "https://github.com/secoya/ansi-logger-js/issues",
	"contributors": [
		{
			"name": "Brian K. Christensen",
			"email": "briankchristensen@gmail.com",
			"url": "https://github.com/thetrompf"
		}
	],
	"dependencies": {
		"cli-color": "1.4.0",
		"moment": "2.22.2",
		"tslib": "1.9.3"
	},
	"description": "Console logger with support for colors and log levels, it complies to the default console.log interface, with methods like log,error,warn,debug and extended with some extra levels for nice formatting purposes.",
	"devDependencies": {
		"@types/jest": "23.3.9",
		"jest": "23.6.0",
		"prettier": "1.15.2",
		"ts-jest": "23.10.4",
		"tslint": "5.11.0",
		"tslint-secoya": "2.0.1",
		"typescript": "3.1.6"
	},
	"engines": {
		"node": ">=8.11.1"
	},
	"keywords": [
		"log",
		"logger",
		"ansi",
		"colors"
	],
	"license": "BSD-3-Clause",
	"main": "lib/index.js",
	"man": [
		"./doc/ansi-logger.1",
		"./doc/ansi-logger.3"
	],
	"name": "ansi-logger",
	"optionalDependencies": {
		"@types/cli-color": "^0.3.29",
		"@types/node": ">=8.10.10"
	},
	"repository": "https://github.com/secoya/ansi-logger-js",
	"scripts": {
		"build": "tsc -p tsconfig.build.json",
		"build-cli": "tsc -p tsconfig.build-cli.json",
		"clean": "rm -Rf lib/ types/*.d.ts",
		"clean-cli": "rm -Rf bin/",
		"fmt": "prettier --write -l 'src/**/*.ts'",
		"lint": "tslint --project .",
		"test": "jest --config jest.config.json",
		"postbuild-cli": "chmod +x bin/cli.js",
		"prebuild": "npm run clean",
		"prepublishOnly": "npm test && npm run lint && npm run build && npm run build-cli"
	},
	"types": "types/index.d.ts",
	"version": "4.1.0"
}
